export enum SortBy {
  POPULAR = 'popular',
  NEWEST = 'newest',
  OLDEST = 'oldest',
  PRICE_ASC = 'priceAsc',
  PRICE_DESC = 'priceDesc',
  LIKED =  'liked'
}

export enum GroupBy {
  GENRE = 'genre',
  ARTIST = 'artist',
  PRICE_RANGE = 'price',
}
